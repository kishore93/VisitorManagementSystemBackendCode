spring:
  datasource:
    url: jdbc:mysql://localhost:3306/visitor-managment?useSSL=false
    username: root
    password: root
  jpa:
    show-sql: true
    open-in-view: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
logging:
  level:
    ROOT: INFO
    org.springframework.web: INFO

    